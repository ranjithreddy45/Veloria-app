# Orphaned & Unreachable Routes

## Overview

Routes discovered in the filesystem that lack direct sidebar links or inbound code references.

---

## Potentially Unlinked Routes

- **`/design-lab`**: Development / design component sandbox (No sidebar entry; `CODE VERIFIED`).
- **`/style-guide`**: UI style guide preview page (No sidebar entry; `CODE VERIFIED`).
- **`/offline`**: PWA offline fallback page (Targeted by service worker; `CODE VERIFIED`).
