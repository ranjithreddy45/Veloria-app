# Feature Dependency Map

## Overview

Maps upstream and downstream module connections.

---

## Module Connections

- **Upstream**: Lead CRM (`Lead`), Venue Directory (`Venue`), Contact DB (`Contact`).
- **Downstream**: Contracts (`Contract`), Bookings (`Booking`), Invoicing & Payments (`Invoice`, `Payment`).
