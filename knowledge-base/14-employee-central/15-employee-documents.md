# 15 Employee Document Management

## Document Schema (`HrDocument`)

Stores employee documents (ID proofs, offer letters, qualification certificates) with S3 storage keys, document category IDs, verification status, and expiry dates. Managed via `src/actions/hr-documents.actions.ts`.
