# 33 Employee Identity & Unified User Model

## Unified Identity Architecture

A physical person is represented by one `Employee` record, which optionally links to one `User` account (`userId`), creating a unified identity across CRM, Bookings, Operations, and HR.
