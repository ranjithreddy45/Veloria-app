# 23 Employee Bulk Import & Export

## CSV Import Engine (`src/actions/hr-import.actions.ts`)

Allows bulk employee upload from CSV templates, validating duplicate `empCode` and `email` before batch insertion.
