# 16 Employee Offer Letters & Contracts

## Recruitment Handoff (`recruit-offer-letter.actions.ts`)

- Recruitment module generates offer letters (`RecOfferLetter`).
- Candidate acceptance triggers `createEmployeeFromCandidate`, copying offer details into `Employee` and `HrSalaryStructure`.
