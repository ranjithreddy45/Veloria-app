# 22 Employee Search & Filtering Engine

## Filtering Parameters (`getEmployees`)

Supports filtering by:
- Search text (Name, empCode, email, phone).
- Department ID.
- Designation ID.
- Employment Status (`ACTIVE`, `ONBOARDING`, etc.).
- Employment Type (`FULL_TIME`, etc.).
