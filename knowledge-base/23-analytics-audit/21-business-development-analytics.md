# 21 Business Development & Property Acquisition Analytics

`CODE VERIFIED`

## Property Pipeline & Projections Analytics (`src/actions/acq-analytics.actions.ts`)

- **Property Acquisition Pipeline**: Deal stages (`LEAD` -> `SITE_VISIT` -> `EVALUATION` -> `PROJECTION` -> `CONTRACT` -> `HANDOVER`).
- **Projected EBITDA & Payback**: Financial feasibility metrics computed in `acq-projection.actions.ts`.
- **Capex Variance**: Estimated capex budget vs actual spent on venue renovation/onboarding.
