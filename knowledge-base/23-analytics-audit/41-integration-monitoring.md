# 41 External Integration Health Monitoring

`CODE VERIFIED`

## Integration Health Monitors

- **Razorpay**: Webhook signature validation logs failures in `ActivityLog`.
- **WhatsApp (Meta / Weflux)**: Outbound API errors recorded in `WhatsAppInboundEvent` and `Communication`.
- **Tally Prime**: Integration sync logs stored in `AccountingSyncLog`.
