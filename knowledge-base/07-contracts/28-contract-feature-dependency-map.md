# Feature Dependency Map

## Overview

Maps upstream and downstream module connections.

---

## Module Connections

- **Upstream**: Sales Quotations (`SalesQuotation`), Lead CRM (`Lead`), Contact DB (`Contact`).
- **Downstream**: Bookings (`Booking`), Invoicing & Advance Deposits (`Invoice`).
