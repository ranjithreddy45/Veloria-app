# 25 Property to Booking Availability

`CODE VERIFIED`

- Once `Venue` is created by `ensureVenueForProperty()`, the venue is immediately available for selection in Sales Quotations (`Quote`) and Event Bookings (`Booking`).
