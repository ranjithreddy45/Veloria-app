# 27 Property to Finance Integration

`CODE VERIFIED`

- Commercial terms (`baseFeePct`, `royaltyPct`, `monthlyRent`) are stored in `AcqDeal` and `AcqProjection`.
- Automatic GL journal entry creation upon property acquisition is `NOT AUTOMATIC` (handled via monthly finance payout runs).
