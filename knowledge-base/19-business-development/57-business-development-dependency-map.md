# 57 BD Dependency Map

`CODE VERIFIED`

- **Automatic Dependencies**: `AcqLead` -> `AcqDeal` -> `ensureDealProperty()` -> `AcqProperty` + `AcqOnboardingProject` + `HallOwner` -> `ensureVenueForProperty()` -> `Venue` -> `Quote` / `Booking`.
- **Manual Dependencies**: Contract Negotiation, Onboarding Task Completion, Financial Projection Parameter Entry.
