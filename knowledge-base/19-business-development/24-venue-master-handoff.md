# 24 Venue Master Handoff

`CODE VERIFIED`

- `Venue` model fields populated from `AcqProperty`: `name`, `description` (address + locality + city), `capacity`, `pricePerSlot` (default 0), `isActive = true`.
