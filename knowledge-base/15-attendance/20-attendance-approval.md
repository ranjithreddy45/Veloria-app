# 20 Attendance Approval & Manager Actions

## Manager Approval Rights

Managers review flagged punches (`getFlaggedPunches`) and regularization queues (`getRegularizationQueue`), with audit logs recording `flagClearedById` and `flagClearNote`.
