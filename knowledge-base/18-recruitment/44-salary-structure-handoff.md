# 44 Salary Structure Handoff

`CODE VERIFIED`

- Handoff is `MANUAL`.
- `createEmployeeFromCandidate()` records offered CTC in `Employee.notes`.
- `HrSalaryStructure` is **NOT** automatically created; HR must define the breakdown (Basic, HRA, Allowances) in Payroll.
