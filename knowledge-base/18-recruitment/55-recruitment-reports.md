# 55 Recruitment Reports

`CODE VERIFIED`

- `getRecruitDashboard()` computes:
  - **Pipeline Matrix**: Counts per stage for active openings.
  - **Time-to-Fill**: Average days from opening `createdAt` to `filledAt`.
  - **Time-to-Hire**: Average days from candidate `createdAt` to `HIRED` application `updatedAt`.
