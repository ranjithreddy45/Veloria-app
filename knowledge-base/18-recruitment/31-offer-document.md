# 31 Offer Document

`CODE VERIFIED`

Generated dynamically via `getOfferLetter(offerId, templateId?)` in `src/actions/recruit-offer-letter.actions.ts`. Merges placeholders (`{{candidateName}}`, `{{jobTitle}}`, `{{ctc}}`, `{{joiningDate}}`, `{{today}}`) into HTML template for printing at `/recruitment/offers/[id]/letter`.
