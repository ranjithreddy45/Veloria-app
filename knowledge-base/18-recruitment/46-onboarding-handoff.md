# 46 Onboarding Handoff

`CODE VERIFIED`

- Handoff is `AUTOMATIC` (best-effort).
- `createEmployeeFromCandidate()` triggers `startOnboarding(employeeId, joiningDate)` from `src/actions/hr-journey.actions.ts`.
