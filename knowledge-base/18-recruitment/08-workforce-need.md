# 08 Workforce Need

`CODE VERIFIED`

- Pre-requisition workforce planning is `NOT IMPLEMENTED` as a separate database model.
- Workforce needs are directly expressed by creating a `RecJobOpening` with `numberOfPositions`.
