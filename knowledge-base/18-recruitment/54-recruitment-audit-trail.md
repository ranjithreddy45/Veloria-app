# 54 Recruitment Audit Trail

`CODE VERIFIED`

- `RecJobOpening`, `RecCandidate`, `RecApplication`, `RecInterview`, `RecOffer`, and `RecBgvCheck` track `createdById` and timestamps (`createdAt`, `updatedAt`).
