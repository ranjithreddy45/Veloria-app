# 38 User Account Handoff

`CODE VERIFIED`

- Handoff is `MANUAL`.
- `createEmployeeFromCandidate()` does **NOT** automatically create a `User` account for system login.
- User creation must be done separately via HR User Provisioning.
