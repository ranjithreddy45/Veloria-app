# 09 CRM Entity Associations

`CODE VERIFIED`

- `WhatsAppMessage` and `Communication` models carry `contactId` and `bookingId` FKs.
- Downstream entity linkage maps messages to Contacts, Leads, Bookings, Vendors, and Employees for complete customer 360 history.
