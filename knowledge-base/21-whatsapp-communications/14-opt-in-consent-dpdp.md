# 14 Opt-In Consent & DPDP Compliance

`CODE VERIFIED`

- Consent evidence recorded via `recordConsent()` in `src/lib/privacy/consent.ts`.
- Saves record in `PrivacyConsentLedger` with `subjectType`, `email`, `phone`, `purpose` (`WHATSAPP_COMMUNICATION`), and `consentText`.
