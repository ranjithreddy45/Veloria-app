# 18 Email Tracking & Analytics

`CODE VERIFIED`

- **Open Tracking**: `EmailTrackingPixel` inserts transparent 1x1 image pixel `<img src="/api/track/open/[pixelId]"/>`.
- **Click Tracking**: `EmailTrackingEvent` wraps links with `/api/track/click/[pixelId]`.
