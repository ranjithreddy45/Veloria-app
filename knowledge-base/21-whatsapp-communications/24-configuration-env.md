# 24 Configuration & Environment Variables

`CODE VERIFIED`

- `WHATSAPP_VERIFY_TOKEN`, `WHATSAPP_APP_SECRET`, `RESEND_API_KEY`, `EMAIL_FROM`, `EMAIL_DOMAIN`.
- Stored securely in `WhatsAppConfig` database model or environment variables.
