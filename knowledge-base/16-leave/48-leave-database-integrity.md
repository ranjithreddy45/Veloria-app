# 48 Database Integrity & Constraints

## Indexes & Unique Keys

- `@@unique([employeeId, leaveTypeId, year])` on `LeaveBalance`.
- Indexes on `[employeeId]`, `[approverId]`, `[status]`, `[startDate]` on `LeaveRequest`.
