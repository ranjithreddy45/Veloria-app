# 31 Leave to Attendance Integration

## Attendance Bridge (`generateAttendanceSheet`)

`generateAttendanceSheet` queries approved paid `LeaveRequest` records for the month, adding working days covered by leave to `leaveDays`.
