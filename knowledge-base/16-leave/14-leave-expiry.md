# 14 Leave Expiry & Reset Rules

## Expiry Logic

Unused Casual Leave (CL) and Sick Leave (SL) expire at fiscal year end (`carryForwardMax = 0`). Only Earned Leave (EL) carries forward up to its configured ceiling.
