# 17 Half-Day Leave Processing

## Half-Day Parameters

`startPart` and `endPart` accept `FIRST_HALF` or `SECOND_HALF`. If `allowHalfDay = true` on `LeaveType`, half-day requests reduce the requested duration by 0.5 day.
