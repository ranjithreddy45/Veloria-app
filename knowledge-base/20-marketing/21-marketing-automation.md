# 21 Marketing Automation

`CODE VERIFIED`

- `GET /api/cron/attribution-rollup`: Runs scheduled attribution aggregation, linking lead booking values back to `LeadAttribution` and updating `MarketingCampaign.bookedRevenue`.
- Auto-push to external CRMs (`pushLeadToWeflux()`, `scheduleAutoPushToCallVibe()`).
