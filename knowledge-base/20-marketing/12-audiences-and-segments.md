# 12 Audiences and Segments

`CODE VERIFIED`

- Email campaigns (`Campaign`) store JSON criteria in `recipientFilter`.
- Supported filter rules: Event Type (Wedding, Corporate, Birthday), City, Lead Quality (`QUALIFIED`, `HOT`), Booking Value range, and Date Range.
- Evaluated dynamically at email dispatch time (`sendCampaign`).
