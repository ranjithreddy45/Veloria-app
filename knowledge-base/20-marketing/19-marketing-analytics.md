# 19 Marketing Analytics

`CODE VERIFIED`

- `getMarketingAttribution()` in `src/actions/marketing-report.actions.ts`:
  - **Metrics**: Total Leads, Qualified Leads, Qualification Rate %, Won Count, Total Booking Value (INR), Average Booking Value.
  - **Grouping**: Grouped by campaign (`gadsCampaignId`, `utmCampaign`, or `MarketingCampaign.name`).
- Dashboard rendered at `/reports/marketing-attribution`.
