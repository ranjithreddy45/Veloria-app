# 23 RBAC and Security

`CODE VERIFIED`

- Permissions: `marketing:read`, `marketing:manage`, `campaigns:read`, `campaigns:write`, `campaigns:send`, `leads:read`.
- Public Intake Protection: Honeypot form field checking, rate limiting per IP and per code, strict Base64/URL validation for resume/document uploads.
